---
phase: 01-monorepo-local-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - docker-compose.yml
autonomous: true
requirements:
  - F1.1
  - F1.2
  - F1.3
  - F1.4
  - F1.5
  - F1.10
must_haves:
  truths:
    - "Monorepo includes @worldcup/mcp under apps/mcp and references it correctly in root tsconfig.json"
    - "Root codegen.ts exists and compiles GraphQL schemas into packages/domain/src/generated/graphql.ts"
    - "Docker Compose defines api, web, worker, mcp, postgres, and redis services with standard port mapping"
  artifacts:
    - "apps/mcp/package.json"
    - "apps/mcp/tsconfig.json"
    - "apps/mcp/src/index.ts"
    - "codegen.ts"
    - "infra/docker/mcp.Dockerfile"
    - ".github/workflows/ci.yml"
  key_links:
    - "Root tsconfig.json project reference pointing to apps/mcp"
    - "apps/web/package.json and apps/api/package.json resolving packages via workspace:*"
---
<objective>
Configure the base monorepo environment by adding the missing apps/mcp service, setting up shared GraphQL type compilation, defining docker compose services for local hot-reload execution, and establishing a CI verification baseline.
</objective>

<execution_context>
@/Users/rony/dev/worldcupPredictor/.opencode/gsd-core/workflows/execute-plan.md
@/Users/rony/dev/worldcupPredictor/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-local-infrastructure/01-CONTEXT.md
@.planning/phases/01-monorepo-local-infrastructure/01-RESEARCH.md
@.planning/phases/01-monorepo-local-infrastructure/01-VALIDATION.md
@.planning/phases/01-monorepo-local-infrastructure/01-PATTERNS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Scaffold apps/mcp and configure workspaces</name>
  <files>
    - apps/mcp/package.json
    - apps/mcp/tsconfig.json
    - apps/mcp/src/index.ts
    - tsconfig.json
  </files>
  <read_first>
    - tsconfig.json
    - apps/worker/package.json
    - apps/worker/tsconfig.json
    - AGENTS.md
  </read_first>
  <action>
    Create the apps/mcp package directory structure. Configure apps/mcp/package.json to have name "@worldcup/mcp", depend on "@modelcontextprotocol/sdk" version 1.29.0 and "@worldcup/domain" version "workspace:*" (per D-01). Add scripts for "dev" (using tsx src/index.ts) and "build" (using tsc). Configure apps/mcp/tsconfig.json extending "../../tsconfig.base.json" with outDir "./dist" and references to "../../packages/config" and "../../packages/domain". Append apps/mcp to references array in root tsconfig.json. Write a minimal server initializer inside apps/mcp/src/index.ts that starts a basic MCP server instance on port 4001 using Server and SSEServerTransport from the SDK. Note that apps/mcp operates as a standalone development service (runnable on port 4001, implementing REQUIREMENTS.md F1.1/F8.1) but its entrypoint and handler must be structured cleanly so that apps/web can import and mount its HTTP/SSE endpoint under /api/mcp (port 3000) for production alignment with AGENTS.md.
  </action>
  <acceptance_criteria>
    - The file apps/mcp/package.json must exist and name must equal "@worldcup/mcp".
    - The file apps/mcp/tsconfig.json must exist and extends must equal "../../tsconfig.base.json".
    - The root tsconfig.json references array must contain an object pointing to "apps/mcp".
    - Running "pnpm --filter @worldcup/mcp... run build" compiles typescript files to "dist/" without error.
  </acceptance_criteria>
  <verify>
    pnpm --filter @worldcup/mcp... run build
  </verify>
  <done>
    apps/mcp is fully scaffolded, compiled using project references, and integrated into pnpm workspace.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Shared config packages & setup GraphQL Codegen</name>
  <files>
    - package.json
    - codegen.ts
    - packages/domain/package.json
  </files>
  <read_first>
    - package.json
    - packages/domain/package.json
    - packages/domain/src/index.ts
  </read_first>
  <action>
    Add "@graphql-codegen/cli" (version 5.0.2), "@graphql-codegen/typescript" (version 4.0.9), and "@graphql-codegen/typescript-resolvers" (version 4.2.1) as devDependencies in root package.json. Run pnpm install at the root workspace. Create codegen.ts in the root (per D-06) configured to read schema definitions in "apps/api/src/**/*.ts" and compile output to "packages/domain/src/generated/graphql.ts" using the "typescript" and "typescript-resolvers" plugins. Update root package.json to expose scripts: "codegen" running "graphql-codegen" and "codegen:watch" running "graphql-codegen --watch". Update packages/domain/package.json to export the generated models file by adding "./src/generated/graphql.js" to exports if needed, or by exporting it from packages/domain/src/index.ts.
  </action>
  <acceptance_criteria>
    - Root package.json must contain devDependencies for @graphql-codegen/cli, @graphql-codegen/typescript, and @graphql-codegen/typescript-resolvers.
    - The file codegen.ts must exist in root directory.
    - Running "pnpm run codegen" succeeds and creates packages/domain/src/generated/graphql.ts containing TypeScript types and resolver signatures.
  </acceptance_criteria>
  <verify>
    pnpm run codegen
  </verify>
  <done>
    GraphQL Code Generator is configured and generates models inside packages/domain/src/generated/graphql.ts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Docker Compose services, volumes & CI workflow</name>
  <files>
    - docker-compose.yml
    - infra/docker/mcp.Dockerfile
    - .github/workflows/ci.yml
    - .env.example
  </files>
  <read_first>
    - docker-compose.yml
    - infra/docker/api.Dockerfile
    - .env.example
  </read_first>
  <action>
    Create infra/docker/mcp.Dockerfile modeled after api.Dockerfile, using node:20-alpine to build the workspace and execute "node apps/mcp/dist/index.js". Update docker-compose.yml (per F1.4, F1.10) to register the "mcp" service on port 4001, mounting the monorepo root volume with "node_modules" excluded via anonymous volume mapping to facilitate hot reloading. Note that apps/mcp is a development service that shares the underlying codebase integrated under /api/mcp on port 3000 in apps/web. Add volume mounts and development-mode watch commands (e.g. tsx watch) to api, web, worker, and mcp service configurations in docker-compose.yml. Document all environment variables inside .env.example (per F1.5), including THESTATSAPI_KEY, DATABASE_URL, REDIS_URL, AUTH_MODE, GRAPHQL_PORT, WEB_PORT, and PROVIDER_MODE. Create .github/workflows/ci.yml configured to run "pnpm install", "pnpm run lint", and "pnpm run typecheck" on any pull request targeting main.
  </action>
  <acceptance_criteria>
    - The file infra/docker/mcp.Dockerfile must exist and map CMD to "apps/mcp/dist/index.js".
    - The file docker-compose.yml must contain services for postgres, redis, api, web, worker, and mcp.
    - The file .env.example must specify all required keys for DB connections, auth modes, and providers.
    - The file .github/workflows/ci.yml must exist and define actions for PR linting and typechecking.
  </acceptance_criteria>
  <verify>
    docker compose config
  </verify>
  <done>
    All services containerized in docker-compose.yml with local volumes mapped for hot reloading, and CI workflow configured.
  </done>
</task>
</tasks>

<threat_model>
- Trust Boundaries: The containerized boundary isolates databases and backend servers from the local host machine, communicating strictly via explicit port mappings.
- STRIDE Register:
  - Information Disclosure: Enforce that .env files containing local secrets are never committed by verifying .gitignore.
  - Denial of Service: Configure Docker Compose memory/CPU resource constraints if needed to prevent local host exhaustion.
</threat_model>

<verification>
Verify monorepo builds and typechecks completely:
1. `pnpm install`
2. `pnpm run codegen`
3. `pnpm run build`
4. `pnpm run lint`
5. `pnpm run typecheck`
</verification>

<success_criteria>
- Workspace builds and typechecks cleanly without errors.
- GraphQL Codegen runs and creates packages/domain/src/generated/graphql.ts.
- `docker-compose.yml` config validation passes.
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-local-infrastructure/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces
### New File Paths
- `apps/mcp/package.json`
- `apps/mcp/tsconfig.json`
- `apps/mcp/src/index.ts`
- `codegen.ts`
- `infra/docker/mcp.Dockerfile`
- `.github/workflows/ci.yml`
- `packages/domain/src/generated/graphql.ts`
