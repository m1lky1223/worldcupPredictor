---
phase: 02-domain-schema-provider-adapters
plan: 01
type: execute
wave: 2
depends_on:
  - 02-02-PLAN.md
files_modified:
  - packages/data-providers/src/index.ts
autonomous: true
requirements:
  - F3.1
  - F3.7
  - F3.9
  - NF-07
must_haves:
  truths:
    - "SyncProvider interface exposes fetchFixtures, fetchTeams, fetchSquads, fetchMatchStats returning normalized types (D-05, D-06)."
    - "TheStatsApiClient implements rate limit parsing, exponential backoff with jitter, and writes raw responses to provider_logs (D-08)."
    - "MockSyncProvider programmatically generates squads (23-26 players with correct positions and ratings) and advances matches (Scheduled -> Live -> Completed) (D-11)."
  artifacts:
    - "packages/data-providers/src/types.ts"
    - "packages/data-providers/src/base-client.ts"
    - "packages/data-providers/src/mock/mock-provider.ts"
    - "packages/data-providers/src/__tests__/mock-provider.test.ts"
    - "packages/data-providers/src/__tests__/base-client.test.ts"
  key_links:
    - "SyncProvider interface and MockSyncProvider export from packages/data-providers entrypoint"
---
<objective>
Scaffold the data provider normalized interfaces, implement the shared HTTP client with rate-limiting and retry capabilities, and build the programmatic local MockSyncProvider for offline simulation.
</objective>

<execution_context>
@/Users/rony/dev/worldcupPredictor/.opencode/gsd-core/workflows/execute-plan.md
@/Users/rony/dev/worldcupPredictor/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-schema-provider-adapters/02-CONTEXT.md
@.planning/phases/02-domain-schema-provider-adapters/02-RESEARCH.md
@.planning/phases/02-domain-schema-provider-adapters/02-PATTERNS.md
@.planning/phases/02-domain-schema-provider-adapters/02-VALIDATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define normalized data interfaces</name>
  <files>
    - packages/data-providers/src/types.ts
  </files>
  <read_first>
    - packages/domain/src/db/schema.ts
    - docs/prd.md
  </read_first>
  <action>
    Create packages/data-providers/src/types.ts and define the following typescript interfaces exactly as specified in the pattern map: NormalizedTeam, NormalizedPlayer, NormalizedMatch, NormalizedMatchStats, NormalizedTeamStats, NormalizedPlayerPerformance, and NormalizedMatchEvent. Use strict typing for all properties, including appropriate union type mappings for statuses (e.g. status: 'Scheduled' | 'Live' | 'Completed') and positions (e.g. position: 'Goalkeeper' | 'Defender' | 'Midfielder' | 'Forward').
  </action>
  <acceptance_criteria>
    - The file packages/data-providers/src/types.ts exists.
    - Typescript compiler compiles packages/data-providers/src/types.ts successfully.
    - All 7 normalized interfaces are declared with strict types (no 'any').
  </acceptance_criteria>
  <verify>
    pnpm --filter @worldcup/data-providers run build
  </verify>
  <done>
    Normalized type interfaces are fully defined in packages/data-providers/src/types.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement shared HTTP client</name>
  <files>
    - packages/data-providers/src/base-client.ts
  </files>
  <read_first>
    - packages/domain/src/db/schema.ts
    - packages/data-providers/package.json
    - AGENTS.md
  </read_first>
  <action>
    Create packages/data-providers/src/base-client.ts. Implement the class TheStatsApiClient. Set the baseUrl parameter to "https://api.thestatsapi.com/v1" and resolve the bearer token from the environment variable THESTATSAPI_KEY. Write a protected request method containing retry logic with exponential backoff and random jitter for HTTP 5xx codes and network drops, and retry behavior for HTTP 429 using the Retry-After header. Limit retries to 3 attempts. On each successful request, asynchronously write the raw payload to the database's provider_logs table (using db.insert(providerLogs) with provider set to "TheStatsAPI" and the entityType parameter) without blocking the primary HTTP response return.
  </action>
  <acceptance_criteria>
    - The file packages/data-providers/src/base-client.ts exists.
    - The stats API client retries up to 3 times on 429 and 5xx statuses.
    - Successful requests write raw payloads into the database provider_logs table.
  </acceptance_criteria>
  <verify>
    pnpm --filter @worldcup/data-providers run build
  </verify>
  <done>
    Shared HTTP client is implemented with rate limiting, retries, and database auditing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement programmatic MockSyncProvider</name>
  <files>
    - packages/data-providers/src/mock/mock-provider.ts
  </files>
  <read_first>
    - packages/data-providers/src/types.ts
    - data/teams.json
  </read_first>
  <action>
    Create packages/data-providers/src/mock/mock-provider.ts. Implement the MockSyncProvider class implementing SyncProvider. For fetchTeams, return normalized representation of the 48 participating countries. For fetchSquads(teamId, providerTeamId), programmatically generate 23-26 players per team with country-appropriate names and a realistic position distribution: 3 Goalkeepers, 8 Defenders, 8 Midfielders, and 5+ Forwards. Assign randomized influence scores (40 to 95) scaled to the team's ELO rating. For fetchFixtures and fetchMatchStats, maintain a step counter that advances match states through Scheduled, Live, and Completed. Provide realistic live events, intermediate scores, and final match statistics (xG, possession) along with performance ratings for completed games.
  </action>
  <acceptance_criteria>
    - The file packages/data-providers/src/mock/mock-provider.ts exists and compiles.
    - MockSyncProvider.fetchSquads generates 23-26 players per team with correct positional representation.
    - MockSyncProvider.fetchFixtures returns match statuses changing according to step progression.
  </acceptance_criteria>
  <verify>
    pnpm --filter @worldcup/data-providers run build
  </verify>
  <done>
    Programmatic MockSyncProvider is completed with squad generation and match replay progression.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update data-providers entrypoint exports</name>
  <files>
    - packages/data-providers/src/index.ts
  </files>
  <read_first>
    - packages/data-providers/src/index.ts
  </read_first>
  <action>
    Modify packages/data-providers/src/index.ts to export the SyncProvider interface containing the following methods: fetchFixtures(): Promise<NormalizedMatch[]>; fetchTeams(): Promise<NormalizedTeam[]>; fetchSquads(teamId: string, providerTeamId: string): Promise<NormalizedPlayer[]>; fetchMatchStats(matchId: number, providerMatchId: string): Promise<NormalizedMatchStats>;. Export all entities from types.ts, base-client.ts, and mock/mock-provider.ts.
  </action>
  <acceptance_criteria>
    - The file packages/data-providers/src/index.ts exists.
    - The SyncProvider interface is declared with the exact signatures.
    - Building the packages/data-providers package compiles without type mismatch errors.
  </acceptance_criteria>
  <verify>
    pnpm --filter @worldcup/data-providers run build
  </verify>
  <done>
    Entrypoint exports and main SyncProvider interface are successfully updated.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write unit tests for HTTP Client and Mock Provider</name>
  <files>
    - packages/data-providers/src/__tests__/mock-provider.test.ts
    - packages/data-providers/src/__tests__/base-client.test.ts
  </files>
  <read_first>
    - packages/data-providers/src/mock/mock-provider.ts
    - packages/data-providers/src/base-client.ts
  </read_first>
  <action>
    Create packages/data-providers/src/__tests__/mock-provider.test.ts and write unit tests using Vitest to verify that fetchSquads returns 23-26 players per team, with exact positions and influence ratings within range. Create packages/data-providers/src/__tests__/base-client.test.ts and verify client retries up to 3 times on 429/5xx and fails with error after maximum attempts are reached.
  </action>
  <acceptance_criteria>
    - Test files exist and are run successfully.
    - Running 'pnpm test' passes all unit tests for the data providers client and mock provider.
  </acceptance_criteria>
  <verify>
    pnpm test
  </verify>
  <done>
    Unit tests are added and pass.
  </done>
</task>
</tasks>

<threat_model>
- Trust Boundaries: The HTTP client interfaces with external networks. Keep the API token protected by retrieving it solely from the environment.
- STRIDE Register:
  - Information Disclosure: Enforce that process.env.THESTATSAPI_KEY is not logged inside print statements or query strings.
</threat_model>

<verification>
Verify build and unit tests pass:
1. `pnpm install`
2. `pnpm --filter @worldcup/data-providers run build`
3. `pnpm test`
</verification>

<success_criteria>
- The data-providers package compiles successfully.
- Vitest unit tests for the client retry mechanism and mock squad generator pass.
</success_criteria>

<output>
Create `.planning/phases/02-domain-schema-provider-adapters/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces
### New File Paths
- `packages/data-providers/src/types.ts`
- `packages/data-providers/src/base-client.ts`
- `packages/data-providers/src/mock/mock-provider.ts`
- `packages/data-providers/src/__tests__/mock-provider.test.ts`
- `packages/data-providers/src/__tests__/base-client.test.ts`
