---
phase: 02-domain-schema-provider-adapters
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/db/schema.ts
  - scripts/db-init.ts
  - scripts/seed.ts
autonomous: true
requirements:
  - TS-01
  - F2.1
  - F2.2
  - F2.3
  - F2.4
  - F2.5
  - F2.6
  - F2.7
  - F2.8
  - F2.9
  - NF-04
  - NF-05
must_haves:
  truths:
    - "Drizzle schema defines venues, matchEvents, playerMatchPerformances, teamMatchStats, predictionInputSnapshots, providerLogs, and modelMetrics tables (D-01, D-02, D-03, D-04)."
    - "Schema adds providerId to teams, players, and matches (D-14, D-15)."
    - "db-init.ts programmatically seeds 104 tournament matches (72 group stage, 32 knockout placeholders) using round-robin pairings and onConflictDoUpdate, without static player/squad seed data (D-12, D-13)."
    - "Database migrations are fully generated and applied to the database instance."
    - "Database triggers prevent UPDATE and DELETE modifications on predictions, ratings_snapshots, prediction_input_snapshots, provider_logs, and model_metrics tables to enforce snapshot immutability."
  artifacts:
    - "packages/domain/migrations/ (new SQL migration files)"
    - "packages/domain/src/__tests__/schema.test.ts"
  key_links:
    - "db-init.ts successfully imports and references expanded tables from packages/domain/src/db/schema.ts"
---
<objective>
Expand the Drizzle schema to capture the full domain model (venues, match performance stats, prediction snapshots, logs, metrics), generate database migrations, apply migrations to Postgres, and populate the database with a complete 104-match tournament structure.
</objective>

<execution_context>
@/Users/rony/dev/worldcupPredictor/.opencode/gsd-core/workflows/execute-plan.md
@/Users/rony/dev/worldcupPredictor/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-schema-provider-adapters/02-CONTEXT.md
@.planning/phases/02-domain-schema-provider-adapters/02-RESEARCH.md
@.planning/phases/02-domain-schema-provider-adapters/02-PATTERNS.md
@.planning/phases/02-domain-schema-provider-adapters/02-VALIDATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Expand Drizzle database schema definitions</name>
  <files>
    - packages/domain/src/db/schema.ts
  </files>
  <read_first>
    - packages/domain/src/db/schema.ts
  </read_first>
  <action>
    Modify packages/domain/src/db/schema.ts.
    1. Add text('provider_id') column as nullable and indexable on teams, players, and matches tables.
    2. Define and export new pgTable entities as specified in the Pattern Map:
       - venues (serial id, text name, text city, text country, text provider_id)
       - matchEvents (serial id, integer match_id references matches.id, text team_id references teams.id, integer player_id references players.id nullable, text event_type, integer minute, integer extra_time_minute, text detail, timestamp created_at)
       - playerMatchPerformances (serial id, integer player_id references players.id, integer match_id references matches.id, text team_id references teams.id, real rating, integer minutes_played, integer goals, integer assists, integer yellow_cards, integer red_cards, integer shots, integer shots_on_target, integer passes_attempted, integer passes_completed, integer tackles, integer interceptions, integer saves, integer goals_conceded, integer clean_sheet, timestamp created_at)
       - teamMatchStats (serial id, text team_id references teams.id, integer match_id references matches.id, real possession, integer shots, integer shots_on_target, integer passes_attempted, integer passes_completed, integer corners, integer fouls, integer yellow_cards, integer red_cards, integer offsides, real expected_goals, timestamp created_at)
       - predictionInputSnapshots (serial id, integer prediction_id references predictions.id, integer home_team_elo, integer away_team_elo, real home_squad_rating, real away_squad_rating, real home_tournament_form, real away_tournament_form, real home_player_availability, real away_player_availability, real market_signal, jsonb input_data, timestamp created_at)
       - providerLogs (serial id, text provider, text entity_type, jsonb raw_jsonb, timestamp created_at)
       - modelMetrics (serial id, timestamp calculated_at, real accuracy, real brier_score, real log_loss, jsonb calibration, text model_version)
  </action>
  <acceptance_criteria>
    - The file packages/domain/src/db/schema.ts exports the new tables.
    - All column definitions conform to the specified TypeScript types and foreign key relationships.
    - Compiling packages/domain succeeds without lint or type errors.
  </acceptance_criteria>
  <verify>
    pnpm --filter @worldcup/domain run build
  </verify>
  <done>
    Drizzle database schemas are fully expanded and exported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate Drizzle migration files</name>
  <files>
    - packages/domain/migrations/
  </files>
  <read_first>
    - packages/domain/src/db/schema.ts
    - packages/domain/drizzle.config.ts
  </read_first>
  <action>
    Generate Drizzle Kit migrations using the command line: 'pnpm --filter @worldcup/domain db:generate' or by running 'npx drizzle-kit generate:pg' inside packages/domain. This translates the typescript schema changes into raw SQL migration files inside packages/domain/migrations/.
  </action>
  <acceptance_criteria>
    - At least one new .sql migration file is generated in packages/domain/migrations/.
    - The generated SQL file correctly defines tables venues, match_events, player_match_performances, team_match_stats, prediction_input_snapshots, provider_logs, and model_metrics.
  </acceptance_criteria>
  <verify>
    git status packages/domain/migrations/
  </verify>
  <done>
    Migration SQL script files are successfully generated.
  </done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply Drizzle migrations/push schema to Postgres</name>
  <files>
    - packages/domain/migrations/
  </files>
  <read_first>
    - packages/domain/migrations/
  </read_first>
  <action>
    Apply the database changes strictly using the migration command: 'node scripts/db-init.ts' to programmatically run Drizzle migrations. Do NOT use drizzle-kit push or db:push, as mixing migration scripts and direct schema pushes on the same Postgres instance is a known Drizzle anti-pattern. Ensure all environments strictly run node scripts/db-init.ts to apply migration files generated in Task 2.
  </action>
  <acceptance_criteria>
    - Database migration command completes successfully with exit code 0.
    - The local Postgres instance is synchronized and contains all new tables and columns.
  </acceptance_criteria>
  <verify>
    node scripts/db-init.ts
  </verify>
  <done>
    Database migrations are successfully applied strictly using db-init.ts.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update database seeding scripts for 104-match structure</name>
  <files>
    - scripts/db-init.ts
    - scripts/seed.ts
  </files>
  <read_first>
    - scripts/db-init.ts
    - scripts/seed.ts
    - data/teams.json
  </read_first>
  <action>
    Modify scripts/db-init.ts and scripts/seed.ts. Replace the fixtures seeding loops to programmatically seed all 104 matches:
    1. 72 Group Stage matches: Loop through the 12 groups (A-L). In each group, pair the 4 teams using round-robin index pairings: [0,1], [2,3], [0,2], [1,3], [0,3], [1,2]. Kickoff dates start June 11, 2026, offset by 3 hours per match. Write matches as Scheduled.
    2. 32 Knockout Stage matches: Programmatically seed matches 73 to 104 with homeTeamId: null and awayTeamId: null, assigning correct stages: Round of 32 (16 matches), Round of 16 (8 matches), Quarterfinals (4 matches), Semifinals (2 matches), Third Place (1 match), and Final (1 match).
    3. Ensure all inserts use onConflictDoUpdate on target matches.matchNumber to make the script completely idempotent.
  </action>
  <acceptance_criteria>
    - Running 'node scripts/db-init.ts' output logs '✅ Seeded 104 matches successfully (72 Group, 32 Knockouts).' or similar.
    - Matches table in Postgres contains exactly 104 rows (72 with team references, 32 with null team references).
  </acceptance_criteria>
  <verify>
    node scripts/db-init.ts
  </verify>
  <done>
    Seeding scripts are updated and populate the full 104-match tournament.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write schema and seeding integration tests</name>
  <files>
    - packages/domain/src/__tests__/schema.test.ts
  </files>
  <read_first>
    - packages/domain/src/db/schema.ts
    - scripts/db-init.ts
  </read_first>
  <action>
    Create packages/domain/src/__tests__/schema.test.ts. Write integration tests using Vitest to assert that the database contains all the new tables (venues, match_events, player_match_performances, etc.) and columns (provider_id). Verify that executing node scripts/db-init.ts populates exactly 48 teams and 104 matches in the database without throwing unique constraint violations. Add integration tests attempting to perform UPDATE or DELETE operations on predictions, ratings_snapshots, prediction_input_snapshots, provider_logs, and model_metrics, and assert that these operations are rejected and throw database exceptions.
  </action>
  <acceptance_criteria>
    - The file packages/domain/src/__tests__/schema.test.ts exists.
    - Running 'pnpm test' executes the schema integration test and passes.
    - Test asserts exact match count of 104 and team count of 48.
    - Test asserts that UPDATE or DELETE queries on any of the five snapshot/log tables are rejected with a database trigger exception.
  </acceptance_criteria>
  <verify>
    pnpm test
  </verify>
  <done>
    Schema, seeding, and snapshot immutability integration tests are verified.
  </done>
</task>

<task type="auto">
  <name>Task 6: Implement database-level snapshot immutability triggers</name>
  <files>
    - packages/domain/migrations/ (custom trigger SQL migration)
    - scripts/db-init.ts
  </files>
  <read_first>
    - packages/domain/src/db/schema.ts
    - scripts/db-init.ts
  </read_first>
  <action>
    Implement database-level checks/triggers to enforce immutability on predictions, ratings_snapshots, prediction_input_snapshots, provider_logs, and model_metrics. Define a PL/pgSQL function (e.g., prevent_snapshot_mutation()) that raises an exception when triggered, and create BEFORE UPDATE OR DELETE triggers on each of these 5 tables. Include these SQL statements in a custom migration file generated in Task 2 or execute them programmatically in scripts/db-init.ts post-migration.
  </action>
  <acceptance_criteria>
    - Trigger definitions are applied to predictions, ratings_snapshots, prediction_input_snapshots, provider_logs, and model_metrics.
    - Database throws an exception if an UPDATE or DELETE query is attempted on any of these tables.
  </acceptance_criteria>
  <verify>
    node scripts/db-init.ts
  </verify>
  <done>
    Snapshot immutability triggers are successfully implemented.
  </done>
</task>
</tasks>

<threat_model>
- Trust Boundaries: Database migrations change column types and tables. Access credentials (DATABASE_URL) are read strictly from environment variables.
- STRIDE Register:
  - Injection: Seeding uses Drizzle ORM parametrized query builders (insert values) avoiding raw string SQL concatenation to prevent SQL injection.
</threat_model>

<verification>
Verify migration execution and database seeding:
1. `pnpm install`
2. `node scripts/db-init.ts`
3. `pnpm test`
</verification>

<success_criteria>
- DB initialized and seeded successfully.
- Vitest integration tests verify exact database row counts (48 teams, 104 matches) and the presence of all tables.
</success_criteria>

<output>
Create `.planning/phases/02-domain-schema-provider-adapters/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces
### New File Paths
- `packages/domain/src/__tests__/schema.test.ts`
- Migrations files inside `packages/domain/migrations/`
