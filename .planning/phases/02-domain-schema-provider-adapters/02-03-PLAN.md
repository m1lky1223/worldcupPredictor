---
phase: 02-domain-schema-provider-adapters
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01-PLAN.md
  - 02-02-PLAN.md
files_modified:
  - packages/data-providers/src/index.ts
autonomous: true
requirements:
  - F3.2
  - F3.3
  - F3.4
  - F3.5
  - F3.8
must_haves:
  truths:
    - "Declarative mapping layers map raw provider fields into normalized domain fields using path resolution (dots) (D-09)."
    - "Entity adapters parse API payloads to NormalizedMatch, NormalizedTeam, NormalizedPlayer, and NormalizedMatchStats, with each entity adapter located in its own file under the provider directory (D-06, D-07)."
    - "Graceful degradation logs warnings and continues instead of throwing errors when optional payload fields are missing (D-10)."
    - "Initially use mock data during development, then switch to real provider data once ingestion pipeline is operational (D-13)."
  artifacts:
    - "packages/data-providers/src/thestatsapi/client.ts"
    - "packages/data-providers/src/thestatsapi/mapper.ts"
    - "packages/data-providers/src/thestatsapi/fixtures.ts"
    - "packages/data-providers/src/thestatsapi/teams.ts"
    - "packages/data-providers/src/thestatsapi/stats.ts"
    - "packages/data-providers/src/__tests__/mapper.test.ts"
    - "packages/data-providers/src/__tests__/integration.test.ts"
  key_links:
    - "All provider adapters exported from packages/data-providers entrypoint index.ts"
---
<objective>
Implement the real data provider adapters for TheStatsAPI utilizing a declarative field-mapping structure, ensuring rate-limiting compliance, retry safety, and robust mapping with graceful warning logs for missing optional fields.
</objective>

<execution_context>
@/Users/rony/dev/worldcupPredictor/.opencode/gsd-core/workflows/execute-plan.md
@/Users/rony/dev/worldcupPredictor/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-domain-schema-provider-adapters/02-CONTEXT.md
@.planning/phases/02-domain-schema-provider-adapters/02-RESEARCH.md
@.planning/phases/02-domain-schema-provider-adapters/02-PATTERNS.md
@.planning/phases/02-domain-schema-provider-adapters/02-VALIDATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement Declarative Normalization Mapper</name>
  <files>
    - packages/data-providers/src/thestatsapi/mapper.ts
  </files>
  <read_first>
    - packages/data-providers/src/types.ts
    - packages/data-providers/src/base-client.ts
  </read_first>
  <action>
    Create packages/data-providers/src/thestatsapi/mapper.ts. Implement helper function getNestedValue(obj, path) to resolve keys split by dot notation (e.g. 'team_home.iso_code'). Implement mapRawEntity(raw, config, entityName) which iterates through configuration properties:
    - If the mapping configuration contains a function, call it with the raw entity and set the output, catching errors, logging a warning, and setting it to null.
    - If it's a string, fetch the nested path. If it resolves to undefined/null, print a warning (console.warn) stating the missing property and set the target property to null (graceful degradation), otherwise assign the value.
    Define and export configurations: fixtureMappingConfig, teamMappingConfig, and playerMappingConfig.
  </action>
  <acceptance_criteria>
    - The file packages/data-providers/src/thestatsapi/mapper.ts exists.
    - getNestedValue correctly resolves deep property paths like "stadium.name".
    - Missing properties trigger console warnings instead of throwing runtime errors.
  </acceptance_criteria>
  <verify>
    pnpm --filter @worldcup/data-providers run build
  </verify>
  <done>
    Declarative mapping engine is fully implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Specific HTTP API Client</name>
  <files>
    - packages/data-providers/src/thestatsapi/client.ts
  </files>
  <read_first>
    - packages/data-providers/src/base-client.ts
  </read_first>
  <action>
    Create packages/data-providers/src/thestatsapi/client.ts. Extend base client TheStatsApiClient to define ConcreteTheStatsApiClient. Declare specific async routing calls:
    - getFixtures(): routes to "/fixtures" (saves under 'fixtures')
    - getTeams(): routes to "/teams" (saves under 'teams')
    - getSquad(providerTeamId: string): routes to "/teams/${providerTeamId}/squad" (saves under 'squads')
    - getMatchPerformance(providerMatchId: string): routes to "/matches/${providerMatchId}/stats" (saves under 'match_performance')
  </action>
  <acceptance_criteria>
    - The file packages/data-providers/src/thestatsapi/client.ts exists.
    - ConcreteTheStatsApiClient extends TheStatsApiClient.
    - Methods route HTTP requests to correct resource URLs on the provider API.
  </acceptance_criteria>
  <verify>
    pnpm --filter @worldcup/data-providers run build
  </verify>
  <done>
    Specific API endpoint client routes are implemented.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement TheStatsAPI Entity Sync Adapters</name>
  <files>
    - packages/data-providers/src/thestatsapi/fixtures.ts
    - packages/data-providers/src/thestatsapi/teams.ts
    - packages/data-providers/src/thestatsapi/stats.ts
  </files>
  <read_first>
    - packages/data-providers/src/thestatsapi/mapper.ts
    - packages/data-providers/src/thestatsapi/client.ts
  </read_first>
  <action>
    Create entity sync adapters inside packages/data-providers/src/thestatsapi/:
    1. fixtures.ts: Implement FixturesAdapter with a fetch() method that queries ConcreteTheStatsApiClient.getFixtures() and maps individual items to NormalizedMatch[].
    2. teams.ts: Implement TeamsAdapter. fetch() queries getTeams() and maps to NormalizedTeam[]. fetchSquads(teamId, providerTeamId) queries getSquad(providerTeamId) and maps items to NormalizedPlayer[] linked via teamId.
    3. stats.ts: Implement StatsAdapter. fetchMatchStats(matchId, providerMatchId) queries getMatchPerformance(providerMatchId) and maps results to NormalizedMatchStats containing NormalizedTeamStats, NormalizedPlayerPerformance[], and NormalizedMatchEvent[].
  </action>
  <acceptance_criteria>
    - All three entity adapter files exist.
    - Adapters successfully transform raw API payloads into the strongly-typed normalized formats.
  </acceptance_criteria>
  <verify>
    pnpm --filter @worldcup/data-providers run build
  </verify>
  <done>
    All TheStatsAPI sync adapters are completed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Export TheStatsAPI adapters in package index</name>
  <files>
    - packages/data-providers/src/index.ts
  </files>
  <read_first>
    - packages/data-providers/src/index.ts
  </read_first>
  <action>
    Modify packages/data-providers/src/index.ts to export everything from thestatsapi client and adapters:
    - export * from "./thestatsapi/client.js";
    - export * from "./thestatsapi/fixtures.js";
    - export * from "./thestatsapi/teams.js";
    - export * from "./thestatsapi/stats.js";
  </action>
  <acceptance_criteria>
    - The file packages/data-providers/src/index.ts exports all adapters.
    - Running build at workspace root succeeds without compilation or typecheck errors.
  </acceptance_criteria>
  <verify>
    pnpm --filter @worldcup/data-providers run build
  </verify>
  <done>
    Sync adapters are exported in the entrypoint file.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write unit and integration tests for adapters and mapper</name>
  <files>
    - packages/data-providers/src/__tests__/mapper.test.ts
    - packages/data-providers/src/__tests__/integration.test.ts
  </files>
  <read_first>
    - packages/data-providers/src/thestatsapi/mapper.ts
    - packages/data-providers/src/thestatsapi/fixtures.ts
  </read_first>
  <action>
    Create packages/data-providers/src/__tests__/mapper.test.ts. Write unit tests using Vitest to verify that mapper maps objects, traverses nested pathways, formats statuses/Dates correctly, and logs console warnings without throwing. Create packages/data-providers/src/__tests__/integration.test.ts. Write tests mapping a complete mocked raw API response, verifying it yields correct types and that database provider logs are generated correctly.
  </action>
  <acceptance_criteria>
    - Test files exist and are run successfully.
    - Running 'pnpm test' passes all unit/integration tests for the mapper and the stats API.
  </acceptance_criteria>
  <verify>
    pnpm test
  </verify>
  <done>
    Mapper and integration tests are verified.
  </done>
</task>
</tasks>

<threat_model>
- Trust Boundaries: Data providers process external JSON. The mapper sanitizes types, converting values into concrete primitives (Date, number, string) before they hit internal services.
- STRIDE Register:
  - Tampering: Enforce that parsing routines throw clean warnings and map values to null for unexpected formatting rather than processing corrupt structure.
</threat_model>

<verification>
Verify build and unit tests pass:
1. `pnpm install`
2. `pnpm run build`
3. `pnpm test`
</verification>

<success_criteria>
- The stats API client and adapters compile successfully.
- Vitest unit tests for the declarative mapper and raw API response parsing pass.
</success_criteria>

<output>
Create `.planning/phases/02-domain-schema-provider-adapters/02-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces
### New File Paths
- `packages/data-providers/src/thestatsapi/client.ts`
- `packages/data-providers/src/thestatsapi/mapper.ts`
- `packages/data-providers/src/thestatsapi/fixtures.ts`
- `packages/data-providers/src/thestatsapi/teams.ts`
- `packages/data-providers/src/thestatsapi/stats.ts`
- `packages/data-providers/src/__tests__/mapper.test.ts`
- `packages/data-providers/src/__tests__/integration.test.ts`
