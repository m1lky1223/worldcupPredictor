version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: worldcup-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: worldcup
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: worldcup-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  db-migrate:
    build:
      context: .
      dockerfile: infra/docker/api.Dockerfile
    container_name: worldcup-db-migrate
    environment:
      DATABASE_URL: postgres://postgres:password@postgres:5432/worldcup
      IS_DOCKER: "true"
    volumes:
      - .:/app
      - /app/node_modules
    command: npx tsx scripts/db-init.ts
    depends_on:
      postgres:
        condition: service_healthy

  api:
    build:
      context: .
      dockerfile: infra/docker/api.Dockerfile
    container_name: worldcup-api
    ports:
      - "4000:4000"
    environment:
      DATABASE_URL: postgres://postgres:password@postgres:5432/worldcup
      REDIS_URL: redis://redis:6379
      GRAPHQL_PORT: 4000
    volumes:
      - .:/app
      - /app/node_modules
    command: npx tsx watch apps/api/src/index.ts
    depends_on:
      db-migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(4000, 'localhost').on('error', () => process.exit(1)).on('connect', () => process.exit(0))"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    build:
      context: .
      dockerfile: infra/docker/web.Dockerfile
    container_name: worldcup-web
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_APP_URL: http://localhost:3000
      GRAPHQL_URL: http://api:4000/graphql
      WEB_PORT: 3000
      PORT: 3000
    volumes:
      - .:/app
      - /app/node_modules
    command: pnpm --filter @worldcup/web dev
    depends_on:
      api:
        condition: service_healthy
      db-migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(3000, 'localhost').on('error', () => process.exit(1)).on('connect', () => process.exit(0))"]
      interval: 5s
      timeout: 5s
      retries: 5

  worker:
    build:
      context: .
      dockerfile: infra/docker/worker.Dockerfile
    container_name: worldcup-worker
    environment:
      DATABASE_URL: postgres://postgres:password@postgres:5432/worldcup
      REDIS_URL: redis://redis:6379
      PROVIDER_MODE: mock
    volumes:
      - .:/app
      - /app/node_modules
    command: npx tsx watch apps/worker/src/index.ts
    depends_on:
      db-migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy

  mcp:
    build:
      context: .
      dockerfile: infra/docker/mcp.Dockerfile
    container_name: worldcup-mcp
    ports:
      - "4001:4001"
    environment:
      DATABASE_URL: postgres://postgres:password@postgres:5432/worldcup
      REDIS_URL: redis://redis:6379
      PORT: 4001
    volumes:
      - .:/app
      - /app/node_modules
    command: npx tsx watch apps/mcp/src/index.ts
    depends_on:
      db-migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('net').connect(4001, 'localhost').on('error', () => process.exit(1)).on('connect', () => process.exit(0))"]
      interval: 5s
      timeout: 5s
      retries: 5

  test:
    build:
      context: .
      dockerfile: infra/docker/test.Dockerfile
    container_name: worldcup-test
    environment:
      DATABASE_URL: postgres://postgres:password@postgres:5432/worldcup
      IS_DOCKER: "true"
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      api:
        condition: service_healthy
      web:
        condition: service_healthy
      mcp:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:
